---
description: Browser-first sample and smoke-test code for Inference Bridge
alwaysApply: true
---

# Browser-first sample code

Canonical rules live in **`AGENTS.md`** at the repo root (§ Browser-first sample code). Follow that section for README snippets, issue examples, smoke tests, and DevTools paste-ready code.

Cursor-only: this rule is `alwaysApply` so the constraint stays active even when `AGENTS.md` is not re-injected in a long session.
