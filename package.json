{
  "name": "inference-bridge",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "package": "node scripts/package.mjs"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
