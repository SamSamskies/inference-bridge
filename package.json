{
  "name": "inference-bridge",
  "version": "0.7.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "package": "node scripts/package.mjs"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
